# Test Repo

## Tasks to try
* Get editor (Visual Studio Code)
* Get Git (Preinstalled)
* Create a basic project (Done)
* Add files, commit (Done)

## Git Commands
* `git init`
* `git add`
* `git commit`
* `git status`
* `git push`
* `git branch`
